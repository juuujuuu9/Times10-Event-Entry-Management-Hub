# Database (Neon or any PostgreSQL)
DATABASE_URL=postgresql://username:password@ep-xxxxx.us-east-1.aws.neon.tech/dbname?sslmode=require

# Email (Resend: https://resend.com/api-keys)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# For testing without a verified domain: use onboarding@resend.dev and only send to the email you signed up to Resend with.
# For production: verify your domain at resend.com/domains, then use e.g. events@yourdomain.com
FROM_EMAIL=onboarding@resend.dev
FROM_NAME=Event Check-In

# CORS (origin of the frontend, e.g. dev server or production URL)
CORS_ORIGIN=http://localhost:5173

# Auth (Google staff login — auth-astro). Required for auth to work.
# Generate a secret: openssl rand -hex 32
AUTH_SECRET=6b8be053a3980cb691f91cccb3760e9a529da4b9b7214f28c175d25ed1610c06
AUTH_TRUST_HOST=true
# In Google Cloud Console → APIs & Services → Credentials → your OAuth 2.0 Client:
# Add to "Authorized redirect URIs": http://localhost:4321/api/auth/callback/google
# For production also add: https://YOUR_PRODUCTION_DOMAIN/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Set to the URL you open in the browser (fixes 403 / "Cross-site... is not valid JSON"). Local: http://localhost:4321
AUTH_URL=http://localhost:4321
# Production: AUTH_URL=https://your-domain.com
# Auth.js v5 also checks NEXTAUTH_URL; set in production (e.g. Vercel) alongside AUTH_URL to fix sign-out redirect to localhost.
# NEXTAUTH_URL=https://your-domain.com
# Optional: staff allowlist (comma-separated). If set, only these can sign in.
# STAFF_EMAILS=admin@example.com,staff@example.com
# ADMIN_EMAILS=admin@example.com
# STAFF_DOMAINS=example.com

# Central hub (multi-event, webhook). Generate key: openssl rand -hex 32
MICROSITE_WEBHOOK_KEY=
# Default event slug for in-app RSVP and legacy QR. Run npm run migrate-events first.
DEFAULT_EVENT_SLUG=default

# Optional
PORT=4321
# QR token validity in days (default 7). Imported/printed QRs must be valid until the event.
# QR_TOKEN_TTL_DAYS=7